{
  "mcpServers": {
    "agentmemory": {
      "type": "http",
      "url": "${AGENTMEMORY_MCP_URL:-http://localhost:3114}",
      "headers": {
        "Authorization": "Bearer ${AGENTMEMORY_SECRET:-}"
      }
    }
  }
}
